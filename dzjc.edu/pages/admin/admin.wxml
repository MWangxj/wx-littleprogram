<view class="body">
  <view class="widgets">
    <block wx:for-items="{{list}}">
      <view class="widgets__item">
        <view id="{{item.id}}">
          <text class="widgets__info-name">{{item.obsname}}</text>
        </view>
      </view>
    </block>
  </view>
</view>